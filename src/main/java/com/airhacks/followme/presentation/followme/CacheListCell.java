package com.airhacks.followme.presentation.followme;

import javafx.scene.control.ListCell;

public class CacheListCell extends ListCell<String> {
//    @Override
//    public void updateItem(Cache cache, boolean empty) {
//        super.updateItem(cache, empty);
//        if (empty) {
//            setText(null);
//            setGraphic(null);
//        } else {
//            setText(cache.getName() + " (" + cache.getDifficulty() + "/" + cache.getTerrain() + " )");
//            setGraphic(null);
//        }
//    }
}